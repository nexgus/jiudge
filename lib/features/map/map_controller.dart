import 'package:flutter/foundation.dart';

/// Holds map camera state (center, zoom, rotation) and orientation handling.
///
/// TODO(spec §1.1 F1, §1.2): wire to the mapsforge_flutter camera; expose
/// continuous zoom + rotation, and survive portrait/landscape changes.
class MapController extends ChangeNotifier {
  // Camera state placeholder; fields land with the mapsforge_flutter integration.
}
